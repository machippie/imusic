# Standards: 0.1
---
# @var imusic_user:description: User to run user-specific commands
# @var imusic_user:value: imusic_user | default(homebrew_user) | default(ansible_user_id)
# imusic_user:

# @var imusic_started:description: Restart app if already running
imusic_started: True

...
